apiVersion: apps/v1
kind: Deployment
metadata:
  name: error-500
  namespace: app-net
spec:
  replicas: 1
  selector:
    matchLabels:
      app: errortest
  template:
    metadata:
      labels:
        app: errortest
    spec:
      imagePullSecrets:
      - name: error-secret
      containers:
      - name: error-container
        image: ghcr.io/saisairepo/loadbalancer:1.1
        ports:
        - containerPort: 9002
        resources:
          requests:
            memory: "4Gi"
            cpu: "2"
          limits:
            memory: "4Gi"
            cpu: "2"

---
apiVersion: v1
kind: Service
metadata:
  name: errorsvc
  namespace: app-net
spec:
  type: LoadBalancer
  selector:
    app: errortest
  ports:
  - protocol: TCP
    port: 80
    targetPort: 9002
